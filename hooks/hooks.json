{
  "description": "Blocks PR create/ready/merge (gh, gh api, GraphQL, MCP) without an APPROVED /objection record for the exact commit. Does nothing in repositories without .objection.json.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "sh \"${CLAUDE_PLUGIN_ROOT}/skills/objection/gate/hook.sh\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
